// Package obs provides observability aggregation: it collects runtime
// statistics from netx (HTTP/WebSocket), event (EventHub), and store
// (SegmentedWAL) into a single ObservabilitySnapshot for inspection
// or export to monitoring systems.
package obs
